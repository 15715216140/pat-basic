
#include <iostream>
#include <string>
#include <cctype>
using namespace std;
 
int main() {
    string s;
    cin >> s;
	if(s[0] = '-')
		cout << '-';
		a[0] = s[1];
		
	for(int i = 3; isnum(s[i])s[i+1] != 'E'; i++) {
		a[i-2] = s[i];
	}
    return 0;
}
